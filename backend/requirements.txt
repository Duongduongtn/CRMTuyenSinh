# Core
# AF3 (2026-06-13): bump 5.0.10 → 5.2.15 LTS. Django 5.0.x EOL April 2025, không
# còn security patch. 5.2 LTS support đến April 2028. Đã verify django-unfold,
# DRF, django-cors-headers, django-solo, django-filter compatible.
# Pin chính xác để CI deterministic — DRF 3.15.2 có 1 deprecation warning với
# Django 5.2 (RemovedInDjango60Warning), fix ở DRF 3.16+ release sau.
Django==5.2.15
djangorestframework==3.15.2
django-filter==24.3
django-cors-headers==4.4.0
django-environ==0.11.2
dj-database-url==2.3.0
psycopg[binary]==3.2.3

# HTTP client (Casso webhook fetch, ZNS OTP gửi).
requests==2.32.3

# Symmetric encryption (Fernet AES-128) cho IntegrationCredential lưu key prod
# trong DB - thay cho SSH paste .env.prod. Key seed qua FERNET_SECRET env.
cryptography==43.0.3

# Admin UI
django-unfold==0.42.0

# Patterns
django-solo==2.4.0          # SiteSettings singleton
Pillow==10.4.0              # ImageField
openpyxl==3.1.5             # Excel export báo cáo Sprint 3 Tuần 6

# PDF (đơn đăng ký Sở GTVT). Trên Windows cần GTK runtime — nếu chưa có endpoint
# fallback HTML preview vẫn hoạt động (xem apps/orders/pdf.py).
weasyprint==62.3

# Async + queue
celery==5.4.0
redis==5.2.0

# Observability (chỉ init khi SENTRY_DSN có giá trị; xem config/settings/prod.py)
sentry-sdk[django]==2.18.0

# Dev tools
django-debug-toolbar==4.4.6
django-extensions==3.2.3
ipython==8.27.0
